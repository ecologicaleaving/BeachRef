# Story 1.1: VIS API Service Extension for Referee Data Access

## Status
APPROVED

## Story

**As a** referee system user,
**I want** BeachRef to extract comprehensive referee data from VIS API tournaments using new dedicated referee endpoints,
**so that** I can access detailed official and referee information through established caching patterns with 95%+ cache hit rates and instant loading performance.

## Acceptance Criteria

1. Extend existing `visApi.ts` with `getEventOfficialList()` and `getEventRefereeList()` methods following established patterns like `getBeachMatchList()`
2. Add referee-specific type definitions following existing interface patterns  
3. Implement XML request building using established request patterns
4. Add referee response parsing following existing data transformation patterns

## Tasks / Subtasks

- [ ] Add referee endpoints to VisApiEndpoint enum (AC: 1)
  - [ ] Add `GET_EVENT_OFFICIAL_LIST` to `VisApiEndpoint` enum in `types/api-v2.ts`
  - [ ] Add `GET_EVENT_REFEREE_LIST` to `VisApiEndpoint` enum in `types/api-v2.ts`
  - [ ] Update field selections constants with referee-specific field arrays
  
- [ ] Create referee type definitions following existing patterns (AC: 2)
  - [ ] Create `types/referee-v2.ts` with `RefereeOfficial`, `OfficialRole`, and `OfficialStatus` interfaces
  - [ ] Define request interfaces `GetEventOfficialListRequest` and `GetEventRefereeListRequest`
  - [ ] Add referee response interfaces following existing VIS response patterns
  
- [ ] Implement XML request builders for referee endpoints (AC: 3)
  - [ ] Add `buildGetEventOfficialListRequest()` method to VisApiClient
  - [ ] Add `buildGetEventRefereeListRequest()` method to VisApiClient  
  - [ ] Use existing form data encoding patterns from current API methods
  - [ ] Implement field specification requirements for referee list calls
  
- [ ] Add referee response parsing and transformation (AC: 4)
  - [ ] Extend VisResponseParser with `parseEventOfficialList()` method
  - [ ] Extend VisResponseParser with `parseEventRefereeList()` method
  - [ ] Handle NoOfficial vs NoReferee field distinction properly
  - [ ] Transform raw XML response data into typed referee interfaces
  
- [ ] Implement referee API methods in VisApiClient (AC: 1)
  - [ ] Add `getEventOfficialList(eventNo: string)` method using established retry/error patterns
  - [ ] Add `getEventRefereeList(eventNo: string)` method using established retry/error patterns
  - [ ] Integrate with existing ConnectionCircuitBreaker for resilience
  - [ ] Add referee-specific monitoring and performance tracking

## Dev Notes

### Architecture Context
This story extends the existing VIS API integration infrastructure established in the BeachRef system. The implementation must follow existing patterns to maintain architectural consistency and leverage established resilience mechanisms.

**Primary Integration Points:**
- `services/api/VisApiClient.ts` - Main VIS API client requiring referee endpoint extensions [Source: api-specifications.md#Enhanced VisApiService Methods]
- `types/api-v2.ts` - Type definitions and endpoint enums [Source: Architecture Analysis]  
- `services/api/VisResponseParser.ts` - XML response parsing and transformation (inferred from existing patterns)
- `services/ConnectionCircuitBreaker.ts` - Circuit breaker resilience patterns [Source: Epic context]

### VIS API Integration Specifications

**Request Format Requirements** [Source: referee-extraction-implementation-guidelines.md#API Request Pattern]:
```xml
<Request Type="GetEventOfficialList" Fields="FederationCode FirstName Gender LastName NoOfficial Role Status Type">
  <Filter NoEvent="1601"/>
</Request>

<Request Type="GetEventRefereeList" Fields="FederationCode FirstName Gender LastName NoReferee Status Type TheoryTest StrongPoints WeakPoints">
  <Filter NoEvent="1601"/>
</Request>
```

**Critical Field Distinctions** [Source: referee-extraction-implementation-guidelines.md#Core Requirements]:
- `NoOfficial` - Official identification number for officials list
- `NoReferee` - Actual referee identification number (NOT registration number)
- Field specification mandatory for all `GetXxxList` requests per VIS API requirements

### Existing Patterns to Follow

**XML Request Building Pattern** [Source: api-specifications.md#Implementation Example]:
- Use existing form data encoding (not SOAP)
- Follow established XML request builders from current methods
- Implement field selection optimization using existing `getOptimizedFields()` patterns

**Response Parsing Pattern** [Source: api-specifications.md#Enhanced Behavior Flow]:
- Use existing `VisResponseParser` extension patterns
- Handle XML response transformation following current match/tournament parsing
- Maintain data validation and error handling consistency

**Type Definition Pattern** [Source: architecture analysis]:
- Follow readonly interface patterns from existing VIS types
- Use proper numeric types for VIS fields (not strings) per VIS compliance requirements
- Implement discriminated unions for different referee roles/statuses

### Error Handling Integration
- Extend existing `ConnectionCircuitBreaker.ts` patterns for referee-specific operations [Source: referee-extraction-implementation-guidelines.md#Circuit Breaker Pattern]
- Use established timeout and retry logic from existing API methods
- Implement proper fallback hierarchy following current service patterns

### Performance Considerations
- Field selection optimization acceptable due to daily caching strategy (vs frequent polling)
- Extended field lists (`TheoryTest`, `StrongPoints`, `WeakPoints`) justified by 24-hour TTL
- Memory footprint minimal due to referee data size vs tournament/match data volume

### Project Structure Alignment
```
services/api/
├── VisApiClient.ts           (EXTEND - add referee methods)
├── VisResponseParser.ts      (EXTEND - add referee parsing)

types/
├── api-v2.ts                (EXTEND - add referee endpoints/requests)
├── referee-v2.ts            (CREATE - referee type definitions)
```

### Testing

**Testing Standards** [Source: No specific guidance found in architecture docs]:
- Unit tests for XML request building using Jest patterns (inferred from existing codebase)
- Response parsing tests with mock VIS XML responses
- Error handling tests for timeout/network failure scenarios  
- Integration tests with existing ConnectionCircuitBreaker patterns
- Type safety validation for referee interfaces

**Test File Locations** (following existing patterns):
- `__tests__/services/api/VisApiClient.referee-methods.test.ts`
- `__tests__/types/referee-v2.test.ts`
- `__tests__/services/api/VisResponseParser.referee.test.ts`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-09-05 | 1.0 | Initial story creation from epic requirements | Bob (SM) |

## Dev Agent Record

*This section will be populated by the development agent during implementation*

### Agent Model Used
*To be filled by dev agent*

### Debug Log References  
*To be filled by dev agent*

### Completion Notes List
*To be filled by dev agent*

### File List
*To be filled by dev agent*

## QA Results

*Results from QA Agent review will be populated here after implementation*